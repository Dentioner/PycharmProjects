# coding: utf-8
done = False
p_total = c_total = 0
while not done:
    'block1.py'
    print 'emm'
    play_again = raw_input('Play Again?-Y/N')
    if play_again.lower().startswith('y'):  # 用lower和startwith是来确定各种各样的yes的，只要首字母为y即可继续游戏
         done = False
    else:
        done = True
# 此代码块用来判断是否多次进行游戏
